{
  "type": "module",
  "name": "interpreter-book",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "vitest --run --reporter verbose",
    "test:debug": "vitest --run --reporter verbose --single-thread --inspect-brk",
    "build": "tsc",
    "dev": "npx ts-node --esm src/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.2.5",
    "typescript": "^5.0.4",
    "vitest": "^0.31.1"
  }
}
